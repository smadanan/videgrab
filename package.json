{
  "name": "vidgrab",
  "version": "1.0.0",
  "description": "Stream videos from YouTube, Instagram, Facebook and 1000+ sites",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev:server": "nodemon server.js",
    "dev:client": "react-scripts start",
    "build": "react-scripts build",
    "render-build": "pip install yt-dlp && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "proxy": "http://localhost:3001",
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version"]
  }
}
